---
trigger: always_on
---
Sempre retornar o código de erro, a mensagem de erro, e detalhes sobre o erro, sem vazer código e stack de chamadas.

Sempre atualizar a documentação da API (OpenAPI, Redoc e Swagger) e dos Requisitos quando sofrer qualquer ajuste.

As respostas da API sempre deve ter o campo nest_step para orientar o Agente o que fazer em seguida.

Na Documentação da API deve indicar se é necesssário ou não autenticação, qual nível de agente, e se é utilizavel por humano.

